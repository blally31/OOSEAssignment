- To compile the program run the makefile using the command 'make'.

- To run the program you can either: (1) edit the ARGS variable in the makefile with the necessary command-line argurments OR
									 (2) run the program using the command 'java Main <propertyFile> <eventFile> <planFile> <startYear> <endYear>'
									  replacing the <placeholders> with the necessary filenames and years.
